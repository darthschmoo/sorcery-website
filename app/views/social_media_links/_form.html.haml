= error_messages( builder.object )

#{image_tag social_media_link_icon( builder.object )}
#{social_media_source_to_name( builder.object )}:
= builder.text_field :link
= builder.hidden_field :source
= builder.hidden_field :author_id
